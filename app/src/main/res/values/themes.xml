<resources>
    <!-- Base application theme. -->
    <style name="Theme.IntroductionToAndroid" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" >?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="LikeButton" parent="Widget.MaterialComponents.Button.TextButton.Icon">
        <item name="icon">@drawable/ic_like_24dp</item>
        <item name="iconTint">@color/color_button</item>
        <item name="minHeight">@dimen/icon_button_min_size</item>
        <item name="minWidth">@dimen/icon_button_min_size</item>
    </style>

    <style name="ShareButton" parent="Widget.MaterialComponents.Button.TextButton.Icon">
        <item name="icon">@drawable/ic_share_border_24dp</item>
        <item name="iconTint">@color/color_button</item>
        <item name="minHeight">@dimen/icon_button_min_size</item>
        <item name="minWidth">@dimen/icon_button_min_size</item>
    </style>

    <style name="MenuButton" parent="Widget.MaterialComponents.Button.TextButton.Icon">
        <item name="icon">@drawable/ic_menu_24dp</item>
        <item name="iconTint">@color/color_menu</item>
        <item name="android:minWidth">0dp</item>
    </style>
</resources>